# ClassGod 更新日志

> 查看更早版本记录请移步 [CHANGELOG_HISTORY.md](./CHANGELOG_HISTORY.md)。

---

## v1.5.42 — 2026-08-19

### 修复与安全
- **Notes 大文本性能**：侧栏预览与自动标题只扫描有界正文，不再对最长 200 万字符的内容反复做全量拆分、替换和 SwiftUI 排版；预览保持明确省略标记。
- **Notes 状态准确性**：标题或正文未变化时不再刷新修改时间、重排列表或触发写盘；搜索结果中删除笔记后只选择仍可见的相邻项，新建笔记会退出旧搜索以确保立即可见。
- **Clipo 设置生效链路**：修复越界数值、快捷键大小写或无效组合被规范化后提前退出的问题；快捷键重注册、历史裁剪、持久化和剪贴板监听切换现在都会按最终有效设置执行。
- **更新元数据信任边界**：检查 Release 时验证 GitHub API 最终响应仍为本仓库固定 HTTPS 端点，Release 页面也必须属于本仓库；畸形预发布版本号会被拒绝，停止或失败后会清理临时进度和错误状态。

### UI / UX
- **Notes 交互反馈**：搜索框新增一键清空；达到 500 条上限时显示本地化提示；新建、选择、置顶和删除只在操作成功时播放对应 SFX 与触感。
- **Clipo 视觉与搜索**：独立窗口强制使用深色语义和当前主题强调色，避免浅色系统模式下原生控件对比度错误；纯空白搜索恢复完整历史，重复选择当前筛选或恢复相同快捷键保持静默。
- **更新状态可读性**：最近检查时间新增明确标签，下载、停止和错误反馈不再残留上一轮进度。
- **SFX / BGM 复核**：全局短音效仍由内存音色生成，项目没有独立持续 BGM；壁纸视频音轨的主播放器、多屏副播放器静音、音量、暂停、循环、观察者与销毁路径复核稳定。

### 验证
- **质量回归**：主应用 201 项、Helper 17 项测试全部通过，并完成 Debug / Release、Analyze、字符串目录、版本一致性、Helper / LaunchDaemon / Widget 嵌入与签名校验。
- 版本号更新为 v1.5.42 (Build 67)

### 说明
- 当前公开构建仍仅提供 Apple Silicon arm64，App 使用 ad-hoc 签名，PKG 未使用 Developer ID Installer 签名，且尚未经过 Apple 公证。
- 首次打开时，macOS 可能要求在“系统设置 → 隐私与安全性”中手动确认。
