# ClassGod 更新日志

> 查看更早版本记录请移步 [CHANGELOG_HISTORY.md](./CHANGELOG_HISTORY.md)。

---

## v1.5.58 — 2026-09-14

### 修复与优化

- **快捷键录制**：按实际按键编码记录组合，修复 Shift + 数字、标点无法录制，以及不同键盘布局下录制结果与实际注册按键不一致的问题。继续保留窗口切换、应用失活、重置与清除时的监听清理。
- **DestinTab 布局**：底部操作按可用宽度切换单行、两列或纵向排列，修复最小窗口与放大界面中文字逐字换行的问题；保留原操作和音效。
- **新增训练工具**：优化心算题目抽样和阅读材料分段，保留题型均衡、唯一题目及 Unicode 边界。回忆卡在保存前检查归档大小，超限时保留上一份可读取文件并提示缩减内容。
- **专注流与错误百科**：专注倒计时使用单调时钟，系统时间调整不会改变剩余时长；保留暂停精度与本地每日统计。修复错误百科首次搜索、加载重试及过期查询结果覆盖问题。
- **列表与系统监控**：复用笔记、任务、课程和剪贴板的筛选与统计结果，限制长文本预览工作量；减少进程用户名重复查询，修复网络采样分段解码及退出进程的旧速率残留。
- **界面与音频复查**：保留蓝红两侧各 11 项功能与主题，检查中英文、最小窗口和 200% 缩放。持续背景音来自视频壁纸；回归音效关闭、叠加上限、静音台、壁纸暂停与播放器清理行为。

### 安装包与兼容性

- **macOS 通用版**：支持 macOS 14.0+ 的 Apple Silicon 和 Intel Mac，提供 `.pkg`、`.dmg` 和解压后包含 `ClassGod.app` 的 `.app.zip`，附 SHA-256 校验文件。
- **Helper 架构修复**：跟随 Xcode 目标架构编译并合并，避免主应用为通用版但 Helper 仅包含构建电脑架构。主应用、Widget、Helper 均含 `arm64` 与 `x86_64`。
- **应用配置修复**：将源码 `Info.plist` 正确合并到应用配置并排除资源副本，恢复 `classgod://` 小组件链接注册和退出时不保留窗口的设置，消除 PKG 中重复的应用记录。
- **签名与平台范围**：应用使用 ad-hoc 签名，PKG 未用 Developer ID Installer 签名，未经过 Apple 公证。当前没有 iOS/iPadOS、Windows、Linux 实现，不能将 macOS 应用直接打包成 IPA。

### 验证

- 主应用 **369 项测试**（354 项逻辑回归、15 项界面与原生测试）及 Helper **17 项测试**通过；中文原生测试另行通过。
- 中英文各 **90 张截图**完成复核，覆盖新增八项工具、蓝红面板及 18 个核心工具窗口的最小尺寸和 200% 缩放。
- 快捷键先复现 Shift 和键盘布局导致的错误，再验证按键编码一致性；两处录制器覆盖窗口切换、应用失活通知、重置 / 清除及重新录制。测试确认鼠标事件送达后再输入，并复用截图窗口、清理视图，避免固定等待及临时窗口累积影响结果。
- Debug / Release 构建、静态分析、字符串目录、版本一致性与 Release ad-hoc 签名校验通过；Release 无编译器警告或错误。
- 发布打包新增 Apple Silicon / Intel 各 **354 项主应用逻辑回归、17 项 Helper 测试**；Intel 测试通过 Rosetta 执行，未进行 Intel 实机硬件验证。三种安装包解包后内容与签名构建逐文件一致，架构、签名、安装位置、系统版本下限和 SHA-256 校验通过。
- 验证基于本机；键盘布局差异通过合成按键事件覆盖，未遍历全部输入法、音频设备和 macOS 组合，也未执行真实风扇写入。

### 版本

- 主应用与 Widget 源码版本统一为 **v1.5.58 (Build 83)**。
- 安装包与下载校验值以 GitHub Releases 为准。
