# ClassGod 更新日志

> 查看更早版本记录请移步 [CHANGELOG_HISTORY.md](./CHANGELOG_HISTORY.md)。

---

## v1.5.59 — 2026-09-17

### 修复与优化

- **壁纸导入**：导入前确认来源为普通文件，复制后检查图片可解码、视频可播放且包含画面轨道。损坏文件、伪装为图片的目录和只有音轨的视频不再被误报为导入成功；失败或取消时清理临时副本，保留原文件。
- **导入反馈**：媒体检查和导入期间显示进度提示，暂时禁用重复打开文件选择器；保留批量导入的失败数量提示。
- **回忆训练**：修复长答案评分后，下一张卡仍停留在旧滚动位置的问题；切换卡片、进入复习和返回卡库时回到内容顶部，覆盖即时与普通动画模式。
- **音效重试**：只有真正启动播放才记录去重时间，避免音效播放失败后，紧接着的重试被误拦截。
- **界面与音频复查**：保留蓝红两侧各 11 项功能，检查中英文、最小窗口和 200% 缩放；持续背景音仍来自视频壁纸，补充原生播放器的静音、音量、暂停和主副屏音频归属回归。

### 验证

- 主应用 **376 项测试**（359 项逻辑回归、17 项界面与原生测试）全部通过；简体中文原生测试 **17 项**另行通过。
- 中英文各 **90 张截图**完成复核，覆盖蓝红面板、新增八项工具与 18 个核心窗口的最小尺寸和 200% 缩放。
- 先复现损坏媒体误导入、长卡片滚动位置残留和音效失败后的重试被拦截，再验证修复；使用临时目录、独立偏好存储、真实图片及编码视频测试导入和恢复。
- 原生快捷键测试为鼠标按下和松开提供独立事件编号、正确压力值与间隔，并等待录制状态就绪，减少快速合成点击导致的偶发失败；继续覆盖窗口切换、应用失活、重置与重新录制。
- Intel 主应用 **359 项逻辑测试**通过 Rosetta 执行；Apple Silicon / Intel 的 Helper 各 **17 项测试**通过。未进行 Intel 实机硬件验证，也未执行真实风扇写入。
- Debug / Release 构建与静态分析通过，无编译器警告或错误；字符串目录、公告迁移和版本一致性校验通过。未遍历全部音频设备、媒体编码与 macOS 组合。

### 安装包与兼容性

- 主应用与 Widget 统一为 **v1.5.59 (Build 84)**，支持 **macOS 14.0+**；主应用、Widget 和 Helper 均包含 Apple Silicon / Intel 双架构。
- 提供通用 `.pkg`、`.dmg`、解压后包含 `ClassGod.app` 的 `.app.zip` 和 SHA-256 校验文件。
- 应用使用 ad-hoc 签名，PKG 未用 Developer ID Installer 签名，未经过 Apple 公证。当前没有可发布的 iOS/iPadOS、Windows 或 Linux 版本。
- 三种安装包解包后的内容与构建产物逐文件一致，版本、架构、可执行权限、签名、安装位置、macOS 版本下限和 SHA-256 校验通过。
